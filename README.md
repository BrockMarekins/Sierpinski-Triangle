# Sierpiński triangle

## Run venv
```shell
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Run script
```shell
python game.py
```

## Change parameters
See `config.py` and change any of parameters.
